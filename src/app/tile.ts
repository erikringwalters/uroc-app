class Tile {
  id: number;
  name: string;
  imagePath: string;
  url: string;

  constructor(name: string, path: string) {
      this.name = name;
      this.imagePath = path;
  }
  // greet() {
  //     return "Hello, " + this.greeting;
  // }
}

//let greeter = new Greeter("world");
